aio-pika==9.4.2
asyncpg==0.29.0
redis[hiredis]==5.0.7
qdrant-client==1.10.1
httpx==0.27.2
feedparser==6.0.11
beautifulsoup4==4.12.3
pydantic==2.8.2
sentence-transformers==3.0.1
click>=8.0.0
spacy==3.7.6
Pillow==10.4.0
imagehash==4.3.1
python-dateutil==2.9.0
prometheus-client==0.20.0
torch==2.3.1+cpu
--extra-index-url https://download.pytorch.org/whl/cpu
# --- Added by audit fixes ---
# HTML-FALLBACK: trafilatura as last-resort content extractor in HTMLCrawlerAgent
# Falls back gracefully if not installed (ImportError caught inline).
trafilatura>=1.6.0
# Optional: playwright for JS-rendered government sites (ECI, MHA React frontends).
# Uncomment and run `playwright install chromium` in the Docker build if needed.
# playwright>=1.40.0
